# AI Decision Coach

A React + TypeScript prototype for a six-step, human-led decision reflection journey. Milestone 1 uses structured mock data only—there is no LLM or other external API.

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL printed by Vite. Use `npm run build` to create a production build.
